import { mockAuthData } from '../../../../mock-data/responses';

import source from '../source';

describe('<%= componentName %> source', () => {
  it('fetches with the correct params', async () => {
    const responseData = await source.fetch<%= componentName %>(mockAuthData, '/path');
    expect(responseData).toMatchSnapshot();
  });
});
