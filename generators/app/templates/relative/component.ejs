'use extensible';

import React, { Component } from 'react';
import { View } from 'react-native';

import { withInstrumentation } from 'cribspot-core/src/components/Instrumentation';
import { TextOnLight as Text } from 'cribspot-core/src/components/semanticUi';

import constants from '../../../constants';
import { lightTheme as style } from '../../../styles';

/**
 * <%= description %>
 */
class <%= componentName %> extends Component {

  static propTypes = {
  };

  static mapStateToProps() {
    return {};
  }

  static mapDispatchToProps() {
    return {
      doSomething() {},
    };
  }

  static sceneKey = constants.sceneKey.<%= componentNameConstant %>;

  static navigationOptions = {
    title: '<%= sceneTitle %>',
  };

  render() {
    return (
      <View style={[style.sceneContainer]}>
        <Text>Hello, <%= componentName %>!</Text>
      </View>
    );
  }

}

export default withInstrumentation(<%= componentName %>);
